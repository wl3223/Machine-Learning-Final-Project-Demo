* Always use ./.venv/bin/python to run python code.
* Always use `uv` for handling environments as well as packages.
* If ./.venv/bin/python doesn't exist, run `uv venv` and then `source ./.venv/bin/activate` to activate a newly created environment.
